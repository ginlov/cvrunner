apiVersion: batch/v1
kind: Job
metadata:
  generateName: cvrunner-job-   # unique per run
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: cvrunner
        image: __IMAGE__            # placeholder
        args: ["-l", "--exp", "__EXP__"]
        resources:
          requests:
            cpu: "2"
            memory: "4Gi"
          limits:
            cpu: "4"
            memory: "8Gi"
